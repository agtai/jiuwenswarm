# Live Voice 语义 t=0 受控并行：同基线 A/B 实测 — 2026-09-04

> 分支 `hx/0903_live_voice_latency`（w3 `2aef1f533` + 4 项 P0/P1 + t=0 受控并行 `2f2b5ef8e`）。
> 对照组与处理组同一部署源 `855fcf8bf`，只差运维开关 `LIVE_VOICE_DIALOGUE_SPECULATION`（off / 默认 on）。
> 数字不是 SLO；n=5，p99≈max；同一小时内 Provider 波动 ±0.3s。

## 1. 设计（按用户定义的受控版）

- **推理提前、执行不提前**：submit 通过身份/权限/重复请求检查后，候选对话推理与语义决策同时启动。候选在 runtime 之外以影子推理运行（自有 `lv-formal-spec-*` 会话），其工具调用在下层适配器的 stream-event rail 上按会话暂停，暂停先于第一次模型调用；候选不存在于 CR 响应、Bridge 派发、journal 效果、history、通知中的任何一处。
- **语义决策仍是最终路由**：对话决策照常派发正式回合；回合预约钉住"接入候选"的 facade，正式回合把候选的缓冲前缀与实时尾巴接过来并恢复工具。任务、澄清、失败、commit/context/工具策略不一致、候选超限或失败、runtime 关闭、回合提前关闭 → 丢弃候选：取消模型工作、中止暂停中的工具，迟到输出无法生效。
- **首批范围**：无待决语义上下文、作用域无 Task（Store 级计数，不增加鉴权读）、非 native 提交；runtime 每会话 2、registry 全局 4 的并发上限，满则串行；任何启动失败都是原串行路径；`LIVE_VOICE_DIALOGUE_SPECULATION=off` 强制串行。
- 理想节省 `min(S, A)`：S 为完整语义决策耗时，A 为 Agent 产出首个可 TTS 片段的耗时。

## 2. 验证

- 单测 19 条（模块 9、runtime 4、registry 6）；两个反向变异体（丢弃不中止工具 → 5 条变红；未使用候选不丢弃 → 1 条变红）。
- 回归：受影响 11 个套件 496 通过，registry 的 61 个既有失败同集合、零新增。

## 3. 方法

- 停服务 → 还原一次性项目 → 部署 `855fcf8bf`，`LIVE_VOICE_DIALOGUE_SPECULATION=off` → 6 档 × 5 轮（short / medium / long / tool / task / clarify）→ 重新部署（开关默认 on）→ 同样 6 档 × 5 轮。
- 口径同前：说完（最后一个非静音帧）→ 下行首帧；浏览器尾巴另算。
- 新增 `clarify` 档："帮我后台处理一下。"，语义契约要求走澄清，是投机的丢弃路径。
- 投机信号由 `scripts/live_voice/latency_speculation_report.py` 从 swarm 日志统计：启动/接入/按原因丢弃、接入时首块提前量、丢弃调用的 completion token（best-effort）。

## 4. 结果

对照组 `logs/lv-latency-spec-off-20260904-150141`（15:01，开关 off，`855fcf8bf`）；处理组 `logs/lv-latency-spec-on-20260904-152900`（15:29，开关 on，`629790c80`）。两次中间的两个修复提交只改投机路径，串行路径不变。

### 4.1 说完→下行首帧（ms，同基线 A/B）

| 档 | 对照 p50 | 对照 p95 | **处理 p50** | **处理 p95** | Δp50 | 成功 |
|---|---:|---:|---:|---:|---:|---|
| short | 5,541 | 6,876 | **4,439** | **5,211** | **−1,102** | 5/5 → 5/5 |
| medium | 8,088 | 8,740 | **7,078** | **8,762** | **−1,010** | 5/5 → 5/5 |
| long | 9,137 | 9,814 | **8,083** | **9,387** | **−1,055** | 5/5 → 5/5 |
| tool | 6,800 | 6,902 | **5,716** | **5,909** | **−1,084** | 4/5 → 5/5 |
| task | 6,522 | 7,040 | 7,327 | 8,167 | +805 | 5/5 → 4/5 |
| clarify | 4,217 | 6,337 | 4,415 | 4,890 | +198 | 5/5 → 3/5 |

对 09-03 基线：short −1,426、medium −7,958、long −8,751、tool −4,882、task −199（p50）。加浏览器尾巴（P1-4 后 ≈0.5s 换算）感知约 short 4.9s、medium 7.6s、long 8.6s、tool 6.2s。

### 4.2 收益落在哪一段（p50 ms，对照 → 处理）

| 段 | short | medium | long | tool |
|---|---:|---:|---:|---:|
| submit RPC（语义在内） | 1,564 → 1,608 | 1,452 → 1,545 | 1,581 → 1,532 | 1,567 → 1,533 |
| submit→首 delta | 643 → **239** | 905 → **241** | 717 → **144** | 816 → **296** |
| submit→chat.final | 1,000 → **334** | 3,769 → **2,517** | 4,789 → **3,478** | 2,372 → **1,278** |
| 闭嘴→EOT / STT / TTS | 三段合计变化 −529 / +20 / +365 / −164，在同小时波动带内 |

语义决策段没有变化，收益全部来自 Agent 的模型工作与决策重叠：候选在 submit 后立刻起跑，决策完成时首块已就绪。对 medium/long 而言 `min(S, A) ≈ S`（约 1.0–1.1s），与设计预期一致。切分脚本按调用次序分类"语义/主模型"，在处理组中候选流先于语义调用启动，其"主模型"列失去意义，本节以驱动器分段为准。

### 4.3 投机报表（处理组 30 轮，`speculation_report.json`）

| 指标 | 值 |
|---|---:|
| 候选启动 / 接入 | 21 / 20 |
| 首块领先决策 p50 / p95 | 469 / 649 ms |
| 丢弃 | 1（`route:task`，task #0，1.9s 模型时间，133 completion tokens） |
| 跳过 | 6（`visible_tasks`：task #1/#3/#4、clarify #0/#3/#4） |
| 工具门触发 `FORMAL_TOOL_EXECUTION_FORBIDDEN` | 0 |
| `UNIFIED_FOREGROUND_EFFECT_MISSING` / Traceback | 0 / 0 |

### 4.4 路由正确性与副作用

- task：`task.create outcome=accepted` 4/4（#2 丢给 STT 供应商超时）。task #0 是本场唯一投机后被丢弃的回合：语义决策为 task → 候选丢弃、工具从未执行、任务照常创建；该轮总用时 8,030（对照 task #0 7,040），丢弃候选与语义调用争抢模型约 +1s，这是首批范围内"作用域尚无 Task 时的创建请求"要付的代价。
- clarify：3 轮成功全部走澄清并念出澄清语；因前面已创建任务，作用域可见 Task>0，候选按规则**未启动**（skipped），所以"澄清丢弃候选"在本场没有生产样本，仅由单测覆盖。
- task 档 +805ms p50 不来自投机（#1/#3/#4 都是串行），来自 STT 段（#0/#4 各 1.25s 对 0.75s）与 n=4 的方差。
- STT 供应商超时本场 3 轮（task #2、clarify #1/#2），与上午同类；对照组 1 轮。

### 4.5 判定

按用户定义的验收项：对话首音 p50/p95 四档全部改善（p50 −1.0~−1.1s，p95 −0.3~−1.7s）；任务/澄清正确率 100%（task 4/4 创建、clarify 3/3 澄清）；丢弃调用成本 1 次 / 133 tokens / 30 轮；任务轮 p95 8,167 对 7,040，归因于 STT 与方差而非投机；错误分支零副作用（0 MISSING、0 工具门触发、0 回溯）。**t=0 受控并行成立，可合入。**

遗留：作用域无 Task 时的首个创建请求会浪费一次候选（本场 1/30）；clarify 的生产样本待作用域无 Task 的会话补测；三次 STT 供应商超时属外部。
