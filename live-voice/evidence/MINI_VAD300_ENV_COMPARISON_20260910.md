# Mini + VAD300：问题记录及跨环境验证

日期：2026-09-10。状态：配置和问题记录已准备；由用户验证，本轮不修复产品问题。

## 本轮边界与决定

用户要求先记录既有问题，再验证 `gpt-realtime-2.1-mini` +
`server-vad-300`，追加新观察，最后由用户在另一个环境对照后决定哪些需要修复。
WebSocket 一并保留：用户报告昨日已在另一环境验证不复现，本轮不再调查或修复。
此前报告中的修复建议不等于本轮实施授权或已确认的跨环境产品缺陷。

执行中澄清：助手曾将“然后验证 mini+vad300”理解为先执行当前环境自动测试，
已完成下述有限测试。用户随后问“为什么是你验证？”，助手据此停止自动验证，
后续交由用户验证。已发生的自动测试作为独立附录保留，不代表用户验收，
也不据此关闭问题或决定修复。当前工作只收尾配置和记录。

本轮拥有的工作：私有启动配置、服务重启、有限真实 Provider/浏览器/Agent 验证、
问题与证据记录。风险是重启中断本地连接，以及现场网络影响测试；保留原会话及项目文件。
依赖现有独立部署、私有 Provider 配置和已安装的固定 AgentCore。
排除产品代码修复、网络/代理/NIC 调整、回退提交、全量候选验收、物理耳机计时及远程推送。
验收要求：核实实际 Mini/VAD300 配置，执行有限真实语音路径，记录结果及缺口；
不能通过配置文件或 ASR/TTS 单独成功宣称完整 Agent 业务通过。

## 基线与证据位置

- 产品源码：`8fdc014b06ac3605aee2ca79ff9e2d851bfd728e`。
- 独立部署：`C:\Users\admin\Desktop\live-voice-mini-deploy-20260910`，无远程/上游。
- 项目：`Live Voice Mini 20260910`，`proj_c5fb567e`；数据目录独立。
- 原会话：`http://127.0.0.1:5173/chat/web_1a08a5297df_a098194abf34`。
- 原诊断：`C:\Users\admin\Downloads\live-voice-diagnostics-2026-09-10T08-27-51-646Z.json`。
- 完整原始时延、错误去重、根因线索和证据哈希：本仓库忽略目录
  `logs/mini-session-analysis-20260910/` 下的 `session-analysis-zh.md`、
  `latency-details.md`、`analysis-details.json`、`input-manifest.json`。
- 原现场配置为 Mini / speed 1.25 / minimal / server-vad-450 / local barge-in off。
  本轮目标仅将端点改为 server-vad-300。300 指 `silence_duration_ms`，
  不是 prefix padding；参见 [官方 VAD 说明](https://developers.openai.com/api/docs/guides/realtime-vad)。
- 历史 [回退对照](../reviews/REALTIME_COMMIT_CONTROL_20260909.md) 保留独立传输证据。
  这些旧记录保留当时结论，本记录拥有用户最新的暂缓修复决定。

## 既有观察（在 VAD300 验证前登记）

所有条目先列为“待跨环境确认”；复现与是否值得修复是两个不同决定。
时间来自原分析。跨进程墙钟差值为近似间隔，浏览器单调时钟为该浏览器指标，均非物理耳机时延。

| ID | 原现场观察与证据 | 当前线索/限制 | 新环境验证与决定 |
| --- | --- | --- | --- |
| M01 | G6 首音前被新 speech_start 取消，浏览器仍等约 4,004.1 ms 后 buffer-wait 超时；未调度首音 | 服务端取消与前端非 playing 分支的清理存在不一致线索；没有原始录音确认是否噪声触发 | 在生成中、首音前说话，检查缓冲是否及时清理、下一轮是否可用；待决定 |
| M02 | 恢复先复用已关闭 activation 1，出现 NATIVE_RUNTIME_CLOSED；activation 2 随后 ready，约 3.18 s 恢复 | 关闭实例复用路径线索；嵌套异常不可重复计作独立故障 | 记录断开后自动恢复链和 ready 时间；待决定 |
| M03 | WebSocket：27 次写暂停；发送最长 3.223 s，排队最长 14.899 s，心跳 RTT 最长 12.297 s；拥塞窗口 loop lag 约 25 ms，Registry 锁最长 0.025 ms | 用户报告另一环境不复现，暂按环境相关观察保留。本次不诊断。a8afe0c 独立旧对照：发送 2.379 s、排队 6.504 s、loop lag 24.4 ms，伴 TCP 重传 | 新环境复测普通问答并关联发送、队列、心跳、loop lag；不得仅因回退无效判定具体网络设备根因 |
| M04 | 已完成任务的调整被 terminal 状态拒绝；没有创建第三个调整任务，原文件未改动。用户“早茶”被模型表述为“下午茶” | terminal 拒绝符合当前状态约束；意图、口头承诺与实际结果需分别核实，不能把拒绝本身认定为缺陷 | 分开测运行中调整、已完成后修改与关键词保真；待用户决定产品预期 |
| M05 | 首个任务指令同时含“不修改任何文件”和“新文档”，实际产生文件；后续保存请求产生第二个任务 | 有指令冲突；两个任务不是同一 RPC 重复提交的证据 | 用无歧义的只读/新建两条指令分开测；待决定 |
| M06 | T1 完成后约 18.046 s 开始播报并被打断，约 109.793 s 后文本回退并 ACK；T2 完成后约 87.941 s 开始播报 | 前台优先和重试影响通知等待，不等于后台执行这么慢；可接受延迟属待定产品判断 | 记录 completed→首次播报→最终 ACK，覆盖前台持续占用和空闲；待决定 |
| M07 | 杭州问答 UI“任务用时”33.91 s 含后续天气活动；本次杭州直接首音 1.283 s，sent→ACK 11.357 s | 展示关联/计时口径线索；不能把33.91 s当首音 | 对照每条输入/响应 ID，核实 UI 用时起止；待决定 |
| M08 | 原报告27条失败事件含重复嵌套、取消、预期延后：terminal synthesis清理、SSE结束后body读取失败、前台占用延后等 | 需按关联 ID 去重；不能宣称27个独立业务故障 | 比较业务完成状态和用户可见症状，再定日志级别/误报是否需处理 |
| M09 | 浏览器诊断只保留4096条（seq1505–5600），前4轮浏览器首音缺失；另有会话之外一行日志交错 | 证据完整性限制，不可推算缺失首音；不证明业务失败 | 导出前后序号/丢弃数并保存日志，确认诊断覆盖范围；待决定 |
| M10 | 启动时后台19000尚未就绪，两次502；实际会话发生于后续稳定启动后 | 启动顺序/就绪噪声，与稍后会话错误无直接因果证据 | 分别检查启动和稳态窗口；待决定 |

原现场参考：6轮有完整浏览器指标的直接问答 EOT→render 为
1.0091、1.2213、1.9365、2.1421、1.0031、1.2831 s，中位数1.2522 s。
两次真实文件任务 executor 用时22.846/23.687 s，Agent用时16.145/18.679 s。
该数据不是与 VAD300 同语料、同环境时段的受控 A/B 基线。
历史同步 Code/SDK 初始化阻塞是另一个已记录未修项，本轮没有重新复现，
不把它计作此次新发现，也不恢复历史修复工作。

## 已准备的运行配置

独立部署源码仍为上述8fdc014b06，私有启动脚本已将 endpoint 改为
`server-vad-300`，其他语音参数保持原值。重启、前端构建、启动路由检查、
Speech TTS→STT 和正式 receipt 检查均通过。
实际 Provider `session.updated` 于09:17:06.374 UTC确认
`native_model=gpt-realtime-2.1-mini`、`native_vad_type=server_vad`、
`vad_silence_ms=300`、speed1.25、minimal；这不是仅检查配置文件。
证据为 `inputs/swarm.log` 的 `configuration_confirmed`，
clock `python-15448-260879002bae` / seq10。

原项目的README和两份已有任务文件前后哈希一致；没有产品代码修复。
私有启动参数接受两份已知任务产物的现有未提交状态，没有删除或提交这些文件。
独立部署无远程/上游；本次不做远程推送。

## 附录：助手已执行的有限自动测试（非用户验收）

- 会话：`http://127.0.0.1:5173/chat/web_1a08a9b35aa_cd05d2061ac6`。
- 时间：2026-09-10 09:17–09:20 UTC；使用独立无头Chrome及合成语音
  MediaStream，不使用用户麦克风，不注入转写/Task RPC，不改变运行时状态制造窗口。
- 原始证据与逐阶段时延在仓库忽略目录 `logs/mini-vad300-20260910/`：
  `input-manifest.json`、`analysis-details.json`、`profile.json`、`profile.html`、
  `inputs/history.jsonl`、`browser-run/events.jsonl` 和每次快照。
  合并27,691条本会话诊断记录，其中3,422条浏览器记录；解析未发现损坏记录。

| 场景 | 已观察结果 | 时延/边界 |
| --- | --- | --- |
| 杭州在哪个省 | 回答浙江，数字音频输出完成 | G1浏览器EOT→render 1,247.3 ms |
| 只读README找标记 | 真实Agent使用glob/list_files/read_file，返回MINI-20260910-READY，文件未改 | G2过渡语EOT→render 1,490.0 ms；Agent round 7,151.551 ms；glob/list_files/read_file分别73.178/32.406/57.528 ms |
| 长回答期间语音打断 | G6已播放后注入“停一下…北京…”，旧回答标记已打断，下一轮回答北京是首都 | G6首音1,120.0 ms；G7下一轮首音930.0 ms；均为浏览器EOT→render |
| 再问三加四 | 服务端会话历史为“三加四等于七”，测试音频事件保留输出 | 最后完整浏览器快照失败，不补算该轮产品首音指标 |

本次服务日志快照无 ERROR/CRITICAL/Traceback，选定会话诊断未发现
failed/timeout/rejected 事件。已知约4s缓冲超时和关闭实例复用错误未在此次观察到，
但未专门覆盖首音前打断或断线恢复，不作排除结论。
此次输入帧排队最长26.583 ms、pending audio最长20 ms；没有复现原14.899s积压。
不额外执行独立网络测试，不从短样本推断WebSocket问题已消失。
未执行本轮文件任务创建/调整、任务完成播报矩阵或物理设备验收，均留给用户。

### 新观察及测试工具限制

| ID | 观察 | 当前分类及后续核实 |
| --- | --- | --- |
| M11 | 5段合成样本产生10条已提交输入；只读长指令拆成“读取README”“告诉我标记”“不要修改”，长回答/打断/算术指令也在停顿处拆开 | 断句体验待用户真人语音复测；样本有停顿，未做450/300同语料对照，不能认定300回归或错误打断 |
| M12 | 同一只读意图出现三段过渡语，最终才播报核验标记 | 可能与分轮输入及receipt/结果呈现有关；真实读取已成功。待用户核实是否属于需要修复的重复反馈/啰嗦，未定位为独立缺陷 |
| T01 | 最后一次测试快照将诊断页合并返回，超过CDP工具4,194,304字节限制，收到1009并触发工具关闭其自有浏览器 | 测试工具限制；与M03产品Provider WebSocket积压分开。最后一轮产品浏览器诊断未取到；既有快照、数字音频和服务端历史保留。未继续重跑 |
| T02 | 首次测试工具启动使用9337端口，超出既有工具9222–9322校验范围，工具退出；改用9299后启动成功 | 助手测试配置错误，无产品会话或业务影响，不作为产品问题 |

自动浏览器已关闭，服务保持Mini+VAD300供用户使用。所有条目仍待用户对照和决定；
上述成功样本不构成稳定时延、全部场景通过或完整用户验收。

## 新环境交接方法

1. 记录源码 HEAD、模型、silence_duration_ms、语速、reasoning、AgentCore、浏览器版本及环境标识；不要混入修复补丁。
2. 用无歧义指令依次测短问答、句内停顿、真实 Agent 只读、明确的新建文件、运行中调整、完成后修改、播放打断及后续问答。
3. 保存每轮诊断和服务日志，标明 session/input/response/task ID；测试通知时区分前台占用与空闲。
4. 按上述 ID 记录“复现/未复现/没覆盖”，再由用户决定“修复/可接受/环境相关误报/继续取证”。一次未复现不等于永久排除。
