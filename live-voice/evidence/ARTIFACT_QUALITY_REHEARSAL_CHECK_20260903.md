# Bounded artifact quality and rehearsal preparation, 2026-09-03

This follow-up is PARTIAL. It does not close the semantic-retirement packet,
complete business journey, cumulative review or physical acceptance.

## Source and owned scope

HEAD at this run: `87248911fde2220be6a97f72f8c0210ac67d5b67`, branch
`hx/0812_live_voice_w3`, ahead 7 / behind 0. The candidate includes the inherited
working-tree integration; HEAD alone is not the deployed candidate. No new
commit or remote update was made in this follow-up.

The Tier-2 repair owns only the dedicated background Code Adapter's generic
result guidance and focused tests. It preserves Task specifications, original
user requirements, frozen replay, file-tool restrictions and sealed old results.
It adds no operation/schema, business classifier or output validator platform.
The guidance distinguishes mandatory constraints from preferences, requires
full source buffers, honors explicit scenario-time references and distinguishes
prose path delimiters from explicitly requested literal filename characters.
Business fixture names, prices, deadlines and option identifiers are test-only.

Final product file `interface_code.py` SHA-256:
`89d1015607981ba7c7a65d8baf0afa47d035f07528a898c4b40963059b1bcac0`.
Probe script SHA-256:
`60169a5e50d08e3f191abe143ad6c49cb3ad84924ead62e9cccb903d7985a8b2`.

## Focused verification

Five final-source tests pass: dedicated/ordinary prompt isolation and idempotency,
missing-builder failure, file-only tool filtering, foreign-root rejection before
adapter execution, and unchanged original instruction with actual artifact sealing.
Command: `.venv/Scripts/python.exe -m pytest --no-cov`, selecting those five cases
from `test_agentserver_modes.py` and `test_project_code_executor.py`, with
`-q --disable-warnings --maxfail=1`. Final log:
`Temp/live-voice-artifact-quality-unit-final-20260903.log` (49.42 seconds).
Ruff passes for the three changed Python surfaces. No broad suite or independent
review was run; those Gates are not granted by this local review.

The real probe uses the production authenticated composition, confirmation owner,
Task Core, configured Agent, file tools and sealed results. Two registered,
isolated projects run the recorded failed instruction and a non-travel maintenance
case. It does not inject semantic decisions or artifact content into an audio
route, and grants no new speech/UI proof. The structured setup/helper is not a
revival of the retired W2 evidence Gate.

## Preserved failures and limits

- `q1`: test driver emitted a timestamp in an unsupported form. Both submissions
  were rejected; the actual Task store has zero Tasks. The driver now emits UTC
  `Z` timestamps. Original log/report remain unchanged.
- `q2`: both Tasks completed and filenames/seals were correct. The maintenance
  case rejected the five-minute violation and accepted the exact deadline, also
  preserving explicitly literal filename marks. The recorded travel report still
  called an option feasible despite its 02:05 arrival against an unchanged 02:00
  cutoff, and substituted runtime time for the source scenario reference. Its
  business result is FAIL. The probe's automated PASS denotes execution/path/
  integrity only; its travel `business_review` explicitly requires reading.
  See `Temp/live-voice-artifact-quality-failed-attempt-review-20260903.json`.
- `q3`: the final-source maintenance calculation still passes (A finishes 12:35,
  invalid; B finishes exactly 12:30, valid). Its Agent `write_file` call omitted
  the closing literal filename mark, producing `《资源.md` instead of `《资源.md》`.
  This case is FAIL and remains open. The file tool faithfully used the supplied
  path; this is an Agent output-fidelity error, not evidence of tool truncation.
  The prior successful case is not presented as stable closure.

The final `q3` recorded travel Task is
`task-aa3d143d3d894d85a296336f20894e87`, Attempt
`attempt-33591252a8bc4526aedf1934db49ee77`. It completed with exactly the requested
`出行方案.md`, SHA-256
`dd65c7ca7957d97b3245056f59eb18772635071f86275e343870f762bdbf80d0`.
The hotel-cutoff option is now conditional in the table, conclusion and
recommendation; the report no longer calls it unconditionally feasible. Full
artifact quality still FAILS: T03 arrival 23:20 plus 85 minutes is written as
01:05 instead of 00:45; morning flight rows use 1,050 while describing the direct
transfer whose stated components total 990. Planning still uses the message time,
distinguishing it from the source's simulated dates/snapshot; this does not prove
the intended scenario-time reference is consistently resolved. Accordingly,
neither the automated travel integrity PASS nor the earlier maintenance PASS
closes business quality. No further repeated model runs were used to chase a PASS.
Both `q3` workers stopped and configuration hashes remained unchanged. The probe
process exited 1 because the final literal-filename case failed.

The old sealed `f2` report remains byte-identical, SHA-256
`f066dd57fe2241b23424a5f6b0b06975b6b05e125f10e723636e1ac9e1d48f19`.
  No failed artifact was renamed or edited to manufacture acceptance.

## Rehearsal environment

The owned `f3` runtime was rebuilt and relaunched on the final product source.
Its manifest SHA-256 is
`a8a803c56767bb1e13231f91dc84b5e55896779c54c36266a9a8498aac47079f`;
the final probe's source manifest is
`7d85aa76e938fc77ecb0a2baea1d568b0aea8601b214497d17517a22532c3248`.
All recorded product/tool source hashes match the workspace. Vite build passed
in 69 seconds. The ordinary WebSocket `session.create` operation created an empty
conversation in the isolated project; its page returns HTML/HTTP 200. This is
deployment/setup evidence only, not microphone/playback acceptance.

The previous owned `f2` runtime was drained and stopped; configuration hashes
remain unchanged. User services on 5173 and the separate `a4` runtime were not
changed. The new runtime and private setup records live under
`Temp/live-voice-functional-runtime-20260903-f3/`.

The earlier real Cascade analysis/delegation proof remains scoped to its recorded
source. No complete A/B/A2, adjustment/cancel, offline/ACK/refresh journey, Native
business audio or fresh physical acceptance was performed here. Performance work
is deferred. Rehearsal availability must not be reported as final delivery or
as only human acceptance remaining.
