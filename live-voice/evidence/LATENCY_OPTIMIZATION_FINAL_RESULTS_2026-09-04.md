# Live Voice 延迟优化：撤回 P0-2、收窄 P0-1、语义调用结构化并行 — 复测 2026-09-04

> 基线：[LATENCY_BASELINE_5x5_2026-09-03.md](LATENCY_BASELINE_5x5_2026-09-03.md)（w3 `69c82b656`）。
> 上一版：[LATENCY_OPTIMIZATION_P0P1_RESULTS_2026-09-03.md](LATENCY_OPTIMIZATION_P0P1_RESULTS_2026-09-03.md)（P0-1/P0-2/P1-3/P1-4，after-安静）。
> 本文记录三项修改各一个提交、一次暴露缺陷的复测、修复提交、以及以修复版为准的最终复测。数字不是 SLO；n=5，p99≈max。

## 1. 提交（相对上一版 `b83eccbdc`）

| 项 | commit | 改动 |
|---|---|---|
| 撤回 P0-2 | `edd5a42e5` | `git revert 02f9c7ebb`：语义解析回到原 schema/payload；上一版已证明紧凑 JSON 无可测收益 |
| 收窄 P0-1 | `e96b533e2` | 口语二次修订的"算术校验"只在答案含**时间/日期/时长/费用**模式（`12:30`、`3 小时`、`¥120`、`5 天`…）且有工具结果时触发；任意数字（"1 个任务"）不再触发；长度修订不变 |
| 语义调用结构化 | `16cc1206e` | 语义模型改流式；`route` 一出现即宣告；dialogue 宣告立即派发 Agent；Runtime 用**呈现暂扣门**把该响应的全部通知扣在进程内，决策确认后按序放行，决策失败/矛盾/超容量则丢弃+压制+走生成打断围栏；只在无待决语义上下文的纯对话回合投机；宣告过的路由对结构性重试有约束力 |
| 修复 | `171448e84` | 决策先于投机派发完成时，已 `release` 的门不再需要暂扣（直接派发）；已 `discard` 的门拒绝派发。见 §4 |
| **撤销** | `d6e40f3f2` | 换基线后上两项为 `f24318682`/`6dc93b1da`，已于 09-04 一并撤销：早宣告只赚 0.15s，暂扣门带来竞争与控制流负担；由 t=0 受控并行（推理提前、执行不提前）作为独立提交替代。§3–§6 的实测记录保留为选型依据 |

单测：流式宣告 6、暂扣门 7、投机派发 5（含两条复现用例）全过；`_publish` 绕过暂扣的变异体使投机用例变红；registry 套件在纯净基线 `69c82b656`、HEAD、带改动三个版本失败的都是**同一批 61 个**用例（差集双向为 0），其余 runtime/p3/continuity/adapter 套件 242 全过。

## 2. 复测方法

- 部署源为本分支 HEAD，受控启动器 `formal-web-validation`，同一数据目录、同一一次性项目（每次部署前停服务→还原项目→确认 60s 内 0 ReAct、0 后台任务）。
- **部署代码归属已实证**：从服务日志取子进程 cwd（数据目录，不在任何源码根），用同一解释器在该 cwd 导入 `jiuwenswarm`，解析到 `jiuwenswarm-lv-latency` 且含新符号（`_route_hint_from_partial`、`PRESENTATION_HOLD_DISCARDED`）。
- 驱动器、语料、每档 5 轮、口径与基线一致（说完→下行首帧；浏览器尾巴另算）。

## 3. 第一次复测（`16cc1206e`，`logs/lv-latency-after-all-20260904-084033`）：暴露缺陷，不作判定

25 轮中 6 轮失败，分两类：

| 类 | 轮 | 现象 | 归属 |
|---|---|---|---|
| A | long #1、tool #4 | submit 返回 `CONFLICT/UNIFIED_FOREGROUND_EFFECT_MISSING`（"Agent outcome promotion lost its exact prepared owner"），+1.7s 内，无 Agent 模型调用 | **本批结构化改动的竞争缺陷**，§4 |
| B | tool #3、task #0/#2/#3（回退）+ long #4、tool #0/#2、task #1（STT 8–20s） | 网关 `live_voice_streaming_recognition_fallback/degradation reason=STREAMING_SPEECH_PROVIDER_TIMEOUT` ×8，08:47:02–08:49:35 | OpenAI 流式 STT 供应商侧超时；昨天两次运行 0 次；与 submit 路径不在同一进程 |

成功轮的对话档（未受 A/B 影响）：short p50 6,068→**5,421**（−648 对上一版；−444 对基线），medium 8,127→**7,770**，long 8,771→**8,555**。

## 4. 缺陷 A 的机制（已复现、已修复）

journal 里失败轮的 effect 行停在 `prepared`、`recovery_json` 为空、语义绑定已写入。用真实 journal 的 registry 夹具复现：解析器宣告 dialogue 后**立即返回**（决策先于投机任务运行），提交确定性地以同一错误失败。链条：门已 `release` → `hold_presentation` 拒绝"已结算的门" → `before_dispatch` 在写 checkpoint 之前失败 → 派发回滚 → 外层 `_run_unified_foreground_effect` 的幂等提升兜底发现 recovery 为空而抛 MISSING，**把真实错误掩盖成了 owner 丢失**。生产里宣告与决策完成只差 ~150ms（§5），这个窗口每几轮就会撞上一次。

修复：已 `release` 的门不装暂扣（此时没有任何东西是临时的）；已 `discard`/失败/取消的门拒绝派发，客户端看到的是语义错误本身。两条复现用例修复前失败、修复后通过。

顺带发现（未改）：`_run_unified_foreground_effect` 的外层提升兜底会把"派发前失败"统一掩盖为 MISSING；建议在兜底前区分 checkpoint 是否已写。

## 5. 结构化并行到底并行了多少

从 swarm 日志按 submit 窗口解析：语义调用（现为流式）在 submit 后 +155ms 启动；Agent 流式调用在语义调用启动后 **+1,375ms（p50，n=16）** 启动；而语义调用整体 p50 ≈ 1,516ms（切分）。即 `route` 字段出现在语义调用开始后约 1.4s——**DeepSeek 的首 token 时延是流式语义调用的地板**，宣告只比决策完成早 ~150ms，short 档 `submit→首 delta` 782→766 几乎不变；本批收益主要来自 submit 往返本身缩短（2,016→1,722）。

要吃到完整的 ~1.5s，只有两条路（暂扣门已经为此准备好了）：
1. **t=0 起并行**：submit 一到就派发 Agent（对话为默认假设），语义决策只做验证；task 路由（约 1/5 回合）会浪费一次被丢弃的 Agent 调用。
2. **前置门**：任务存储无任务且无待决上下文时，对话是唯一可能路由，跳过语义模型。

## 6. 最终复测（`171448e84`，修复版，`logs/lv-latency-after-fix-20260904-090029`）——**判定以此为准**

24/25 成功；唯一失败 task #3 是 STT 供应商超时回退（`STREAMING_SPEECH_PROVIDER_TIMEOUT` ×2，与代码无关）。新代码信号全零：投机丢弃 0、路由不一致 0、暂扣异常 0、`UNIFIED_FOREGROUND_EFFECT_MISSING` 0、回溯 0。

### 6.1 总用时（说完→下行首帧，ms）

| 档 | 基线 p50 | 上一版 p50 | **最终 p50** | Δ对基线 | Δ对上一版 | 最终 mean | 最终 p99 | 成功 |
|---|---:|---:|---:|---:|---:|---:|---:|---|
| short | 5,865 | 6,068 | **5,556** | **−309** | −512 | 5,610 | 5,952 | 5/5 |
| medium | 15,036 | 8,127 | **7,939** | **−7,097** | −188 | 8,114 | 8,815 | 5/5 |
| long | 16,834 | 8,771 | **9,094** | **−7,740** | +323 | 8,881 | 9,282 | 5/5 |
| tool | 10,598 | 7,788 | **6,784** | **−3,814** | −1,004 | 6,673 | 6,888 | 5/5 |
| task | 7,526 | 10,826 | 14,077 | +6,552 | +3,251 | 16,434 | 29,363 | 4/5 |

用户感知 p50（+浏览器尾巴；P1-4 后 ≈0.5s 为换算、未实测）：short ≈6.1s，medium ≈8.4s，long ≈9.6s，tool ≈7.3s。

### 6.2 分段（p50 ms，基线 → 最终）

| 段 | short | medium | long | tool | task | 判定 |
|---|---:|---:|---:|---:|---:|---|
| 闭嘴→EOT | 2071→**1424** | 2133→**1518** | 2027→**1457** | 2146→**1332** | 2011→**1512** | P1-3 全档 −0.5~−0.8s，稳定复现 |
| STT final | 444→862 | 501→739 | 469→887 | 502→752 | 510→736 | Provider 本时段偏慢 +0.2~0.4s |
| submit RPC（语义在内） | 1561→1587 | 1784→1654 | 1593→1550 | 1510→1508 | 2888→2726 | 结构化改动：**持平**（§5：首 token 地板） |
| submit→首 delta | 785→688 | 676→682 | 698→670 | 1974→837 | 1096→757 | 早宣告 −0.1s 量级；tool/task 的下降来自答案不再被修订/校验 |
| 主模型后→final（修订等） | — | 6984→**1027** | 9240→**1416** | 4158→**1066** | —→6779 | P0-1：修订超时 3/15→**0**；tool 4→3 次调用；task 见 6.3 |
| TTS 首块 | 587→593 | 706→418 | 584→545 | 591→657 | 642→1040 | Provider |

### 6.3 task 档（p50 14.1s）为什么不能记为本批退化

**基线的 task 档不是任务创建回合**：基线日志（`swarm-20260903-202936.log`）显示 5 轮全部 `semantic_delegation_check authorized=True` → `operation=task.create outcome=rejected reason=TASK_CONTEXT_WORKTREE_DIRTY`——当时一次性项目已被前面的场景写脏，创建被拒，agent 只念了一句拒绝语（1 次流式 + 语义/委派 2 次一元），所以 7.5s。之后每次运行都在部署前把项目还原干净：第一轮创建成功（任务库每次运行只多 1 条），后四轮被委派校验判为重复（`authorized=False`）转成前台带工具的对话回合、agent 自己在前台执行请求（每轮 6–16 次流式调用、ReAct 迭代到 12，task #2 的"主模型后→final"23.4s）。所以 +6.5s 是**两种产品行为之间的差**，不是四项改动造成的；属于本批的段没有退化（submit RPC 2888→2726、首 delta 1096→757），P0-1 收窄后 task 答案也不再触发算术校验。真正值得立单的是产品行为本身：被拒绝的委派会静默变成 8–23s 的前台执行，用户在这段时间听不到任何反馈。

### 6.4 逐项判定

- **撤回 P0-2**：无损（语义调用 1394→1398）。
- **收窄 P0-1**：tool 档对上一版 −1.0s、p99 收紧到 6.9s；全部档修订超时 0；task 的"含数字即算术"尾巴消失。
- **结构化并行**：机制在生产中正确运行（20 个窗口里 Agent 均在语义调用开始后 +1.38s 起跑，0 丢弃、0 冲突、24/25 成功），但因首 token 地板只兑现 ~0.1s（首 delta −97ms），submit 往返对基线持平。**它的价值是把暂扣门这套基础设施立起来了**——下一步的 t=0 并行或前置门（§5）才是把 ~1.5s 拿回来的动作。
- 本次修复的竞争缺陷（§4）在修复前每 12 轮撞 2 次，修复后 0 次。

### 6.5 结论与下一步

1. 合入 P0-1 收窄（换基线后为 `fed002089`）；P0-2 已在重放时连同其撤回一起丢弃；语义并行两项已撤销（`d6e40f3f2`）。对基线的收益 medium −7.1s、long −7.7s、tool −3.8s 来自 P0-1/P1-3，不依赖被撤销的两项。
2. 语义调用的下一刀已选定：t=0 受控并行（候选推理从 submit 起跑，工具/Task/history/音频全部扣在决策之后，语义决策仍是最终路由；无 Task、无待决上下文时启用）。前置门不采用：无任务也可能是创建或需澄清的请求，语义契约要求走澄清而非对话兜底。理想节省 min(S, A)，验收看对话首音 p50/p95、任务/澄清正确率、丢弃调用成本、任务轮 p95、错误分支零副作用。
3. 待用户验收：真实 Chrome 复听 P1-4（断音/重缓冲）、VAD 800ms 中文换气语料回归。
4. 独立缺陷单：正式路径无界 ReAct 循环（本次 task #2 仍见 `iteration 12/9223372036854775807`）；`_run_unified_foreground_effect` 外层提升兜底掩盖派发前失败。

## 7. 环境副作用（必须知道）

- 工作树 `.venv` 是指向主仓库 `.venv` 的 junction。受控启动器在启动树跑 `uv sync`，把**共享** venv 的 editable 映射改到了 `jiuwenswarm-lv-latency`（自 09-03 21:31）。服务子进程 cwd 是数据目录，包解析完全由该映射决定：09-03 22:35、22:46 与 09-04 00:04 有人从主仓库启动的三次服务，实际跑的都是本分支的服务端代码（同 cwd、主仓库解释器复现导入证实）。回主仓库工作前请在主仓库根 `uv sync`。
- 09-04 08:33 我的重新部署停掉了当时从主仓库启动的服务（00:04 启动）。
- 主仓库 w3 远端已前进 7 个提交（`69c82b656..bfef2b3ad`），大改 `task_semantics.py`（−195 行）与 registry；本分支迁移时 `16cc1206e` 必冲突。

## 8. 换基线到 w3 `2aef1f533` 并补测 task（11:15，`logs/lv-latency-w3b-task-20260904-111457`）

**重放**：丢弃 P0-2 及其撤回，其余 16 个提交 cherry-pick 到 `2aef1f533`（备份 `backup/hx-0903-pre-rebase-20260904`）。两处冲突：`streaming_speech.py` 的 import（上游 `2aef1f533`）；`task_semantics.py` 与 registry——上游删掉了独立的委派校验与调整校验（`c4987f028`），并在对话分支加入"任务真相"上下文注入。适配一条规则：**上下文含任务时不宣告路由**（投机派发用的是原始 commit，不能绕过任务真相注入；新增用例）。验证：10 个套件 480 通过，registry 61 个失败与上游纯净版同集合。已强推。

**测法修正**（`5f011ec99`）：task 每轮一句不同的只读分析请求。写文件的任务会弄脏项目，之后每次创建都被 `TASK_CONTEXT_WORKTREE_DIRTY` 拒绝——这正是 09-03 基线失效的原因。

**task 创建回合（4/5 真实创建；#1 丢给 STT 供应商超时）**

| 指标 | 09-03"基线"（创建被拒） | **新 w3 真实创建** | Δ |
|---|---:|---:|---:|
| TOTAL p50 / mean / max | 7,526 / 7,539 / 7,933 | **6,837 / 6,913 / 7,309** | −689 |
| 闭嘴→EOT | 2,011 | 1,485 | −526（P1-3） |
| STT final | 510 | 753 | +242（Provider） |
| submit RPC | 2,888 | 2,464 | −424 |
| 其中语义调用（切分） | 2,706 | 2,046 | −661（上游单次合并，`semantic_delegation_check` 0 次） |
| submit→首 delta | 1,096 | 1,096 | 0 |
| submit→final（确认语） | 1,451 | 1,750 | +299 |
| TTS 首块 | 642 | 584 | −58 |

任务库确认本窗口新增 4 条，`task.create outcome=accepted` ×4；新代码信号全零。创建回合的自有开销现在是 EOT 1.5s + 语义 2.0s + 确认语 1.1–1.8s；语义调用带任务上下文比对话档慢 1s，是下一刀（前置门/t=0 并行）之外的另一处。

**short 锚点（新 w3）**：语义调用 1,398→**1,003**ms、submit 1,587→1,253（上游重写了提示与 schema）；总 p50 6,307 受今天 provider 抽风拖累（#4 STT 15.2s、#2 TTS 2.0s），4 轮干净数据 p50 ≈ 5.8s。今天 STT 供应商超时共出现三段，任何单日 n=5 的 short 数字都要看 STT 段再下判断。

顺带：4 个新建后台任务里 2 个在数秒内 `failed`（一次性项目里没有 Python 文件之类的原因待查），与延迟无关。
