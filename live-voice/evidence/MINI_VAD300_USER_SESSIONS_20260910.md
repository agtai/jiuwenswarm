# Mini + VAD300：两次用户会话分析

日期：2026-09-10。状态：已分析用户提供的实测证据；问题记录完成，修复和跨环境分类仍待用户决定。

## 边界与证据

本轮行为：仅离线读取用户提供的两份诊断、对应会话历史、服务日志、持久任务/Work 状态和已生成文件，分析时延、错误与对话。拥有分析产物和文档，不拥有产品行为变更。依赖已有独立部署的历史证据；风险是浏览器缓冲覆盖、跨会话混入和重复异常造成误判。验收为逐会话正确关联、时钟口径明确、事实与推断分开、证据缺口明示。

用户此前明确由自己验证；本轮没有发起新语音/浏览器/Agent 测试，没有修改产品代码、配置或重启服务。未调查网络环境或实施修复。此记录承接 [Mini/VAD300 比较记录](MINI_VAD300_ENV_COMPARISON_20260910.md)，沿用 M01–M12，新增 M13–M17。未关闭旧问题，不恢复历史修复授权。

按用户提供的顺序编号；第二个会话实际更早发生。下文时间均为 UTC，巴黎本地时间加 2 小时。

| 项目 | S1：第一个 | S2：第二个 |
| --- | --- | --- |
| session ID | web_1a08abee5a5_96e85ce3d265 | web_1a08aba3b86_923ef1d97ff0 |
| 用户入口 | [S1 会话](http://127.0.0.1:5173/chat/web_1a08abee5a5_96e85ce3d265) | [S2 会话](http://127.0.0.1:5173/chat/web_1a08aba3b86_923ef1d97ff0) |
| 实际语音窗口 | 09:56–10:01 | 09:51–09:55，10:02 重开导出 |
| 导出文件 | live-voice-diagnostics-2026-09-10T10-01-18-235Z.json | live-voice-diagnostics-2026-09-10T10-02-42-325Z.json |
| 最终用户转写条数 | 16 | 13 |
| Task | task-cd5ad0a1e2e641a7a15afcfb52a958f9 | task-92690d27e63a40008b91968429adf834 |
| 文件 | 深圳两日行程设计.md，7,882 字节 | 深圳两日行程规划.md，4,885 字节 |

独立部署源码为 `8fdc014b06ac3605aee2ca79ff9e2d851bfd728e`，使用独立 Mini 项目 `proj_c5fb567e`。Provider 日志分别于 S1 09:56:03.975、S2 09:50:58.422/09:55:20.620 确认 `gpt-realtime-2.1-mini`、`server_vad`、`silence_duration_ms=300`，并非仅检查配置文件。其他私有运行参数延续比较记录。

本机完整证据在仓库忽略目录 `logs/human-vad300-analysis-20260910/`：

- `analysis-zh.md`：详细结论、根因、错误去重和证据限制。
- `latency-details.md`：29 条用户转写逐项对话、每个响应的阶段、全部 Native 业务调用、20 次 Agent 工具、18 个模型流及阶段统计。
- `S1/`、`S2/`：`analysis-details.json`、`profile.json/html`、`spans.csv`、`trace.json`、原始关联诊断。
- `durable-business-evidence.json`、`business-receipts.json`：任务最终命令、事件消费 ACK、Work 正文与 Native 回执。
- `input-manifest.json`：42 个输入快照的大小与 SHA256；SQLite 使用只读源连接 backup。原始日志和数据库不提交到 Git。

## 数据完整性限制

两份导出使用同一个浏览器 clock_id，是全局环形缓冲内容。第二份导出大部分属于后发生的 S1，不能按文件名将全部记录归给 S2。按 `(clock_id, sequence)` 去重并严格按会话和关联 ID 选取后：

- S1 共 53,607 条诊断，浏览器 4,271 条，可用浏览器窗口从 09:58:13.645 开始；早期首音已被覆盖。
- S2 共 39,670 条诊断，浏览器仅 22 条，全部在 10:02:36.988–10:02:42.084，原语音窗口浏览器首音无法计算。
- 两份导出的 memory_overwrites 分别为 12,581、12,813，overwritten_pages 为 668、675；一行服务诊断 JSON 损坏，已单独记录。
- 没有原始录音，不能核定用户实际字词、停顿或物理耳机首音。没有同语料 VAD450/300 A/B，不能把两个环境时段的差异归因为 VAD。

阶段计时采用同一进程单调时钟。跨进程业务状态间隔为近似墙钟差值；转写落库不是说话结束。中断响应未写入最终已听历史，不等于没有生成或播放音频。

## 时延摘要

| 指标 | S1 | S2 |
| --- | --- | --- |
| 浏览器 EOT→render，直接回应 | n=7，中位 1.291 s，最小 0.808 s，最大 3.434 s | 原语音窗口缺失，不能计算 |
| 网关端点观察→首帧下行，直接回应 | n=16，中位 0.832 s，范围 0.592–2.335 s | n=11，中位 0.810 s，范围 0.707–1.337 s |
| 看海/天气 Work 受理→完成 | 约 5.357 / 7.277 s | 约 4.139 / 5.404 s |
| 文件 Task 受理→终态 | 约 40.855 s | 约 26.593 s |
| executor.run_attempt / Agent 流 | 39.904 / 27.698 s | 26.080 / 19.922 s |
| Native task.adjust 处理 | 5.054 s，初始 dispatched/pending | 0.380 s，终态拒绝 |
| task_query 最长 | 4.323 s（该次浏览器 RPC 4.574 s） | 3.603 s |
| 输入帧排队最长 | 161.593 ms | 58.633 ms |
| 输入发送采样最长 / 心跳 RTT 最大 | 282.649 / 734 ms | 10.018 / 218 ms |
| 写暂停记录 | 0 | 0 |

直接回应包含过渡语，不能当成业务结果交付。网关端点和浏览器 EOT 起点不同，指标不可互换，不能机械补加 300 ms；软件 render 不是耳机物理首音。

重要长尾：

- S2 看海 Work 完成→结果首次下行约 44.198 s；G8 首音已生成后等约 17.809 s 才下行，前台回应占用输出。天气通知继续排队后被“好了”打断。
- S2 Task 完成→通知下行约 74.314 s、→消费 ACK 约 80.069 s，包含 09:54:41 退出至 09:55:20 重开的窗口，不能全部称为活跃连接卡顿。
- S1 Task 完成约 6.132 s 后通知 G19 首帧下行，但浏览器没有调度首音就被打断；快照内没有此 Task 完成通知消费 ACK。
- S1 G20 首音 3.434 s：浏览器 EOT→PCM 2.311 s、PCM→调度 1.090 s、调度→render 32.9 ms。同窗口通知失败处理持 Registry 锁 1,079.411 ms，通知/task_query 分别等锁 994.129/839.618 ms。锁等待与音频阶段有重叠，不能相加，也未证明全部首音慢都由该锁引起。
- 最长 task_query 是后台 UI 状态查询，并非 Mini 的新 Native 业务调用；4.323/3.603 s 的完整内部原因尚未闭合。S2 另一次 1.198 s 等锁发生于 10:02 重开导出，单独记录。
- S1 最后请求全文：过渡语首音 1.291 s；task.result 1.338 s；后续 G24 sent→下行 8.522 s、sent→整段播报 ACK 54.779 s，最终仍未交付文件正文。

## 对话与问题登记

| ID | 已观察事实 | 根因、限制与后续判断 |
| --- | --- | --- |
| M04（S2） | 新增火锅、早茶未写入文件 | 任务 09:54:09.954 完成，09:54:20.695 才受理修改；返回 TERMINAL_TASK_IMMUTABLE。拒绝符合当前终态规则，用户修改目标未达成；后续修改方式待定 |
| M04（S1 新变体） | 新增海鲜、火锅未写入文件 | 修改请求进入处理后，任务完成事实先于调整生效；初始回执 dispatched/pending，最终命令 CONFLICT / TASK_TERMINAL_BEFORE_ADJUSTMENT。任务事件 seq 是消费顺序，occurred_at 是事实时间，不应按墙钟重排成负时延 |
| **M13 新：正文/形式交付** | S1“输出文件全部内容，文字即可”，最终只有任务摘要解释且继续语音 | 仅 task.result，没有真实 Agent/read_file；回执无正文，路由在摘要处停止。文件确实存在，不是 Task 创建失败 |
| **M14 新：产物保真** | S1“找一个咖啡馆”只有片区建议；结果还把旧文件的深圳湾条件称为当前冲突 | 委派指令增加来源限制，产物明确不指定店名。读取既有项目文件后出现旧条件影响判断的线索，不等于协议层跨会话串线 |
| **M15 新：查询/清理长尾** | S1 通知失败处理持共享锁约 1.079 s；两会话状态查询最长 4.323/3.603 s | 已定位慢临界区 handler=handle_p2_presentation_failed；查询完整长尾还有未细分等待。不能统称为旧 WS 输入阻塞 |
| **M16 新：路径交付未闭合** | S2 最后只有 context.get；后续音频有下行，无完整已听正文或 ACK | 浏览器证据缺失且用户退出，不能确认是否给出正确完整路径，列为待补证据 |
| **M17 新：结果内容质量** | S1 天气 Work 将 13、14 日称为周末；S2 天气数据地点/日期可靠性受限 | 2026-09-13 为周日，09-14 为周一。后台正文有内容问题，播报被打断，不宣称这些错误均被用户听到；不将日志天气当当前事实 |
| M06 通知排队 | 看海/天气结果和任务通知存在延后 | 与前台占用、退出窗口分开计时；业务执行完成不等于通知已听到 |
| M09 诊断覆盖 | S2 导出主要混入 S1，S2 语音浏览器证据已覆盖 | 诊断收集范围/保留能力观察，不是会话业务串线证据 |
| M11/M12 分段/过渡语 | S1 行程分三段；S2“完整路径”分两条；“看好/抗海/看海”连续追问 | 第一次 work.get 实际选天气；无录音不能证明 ASR 错字或 VAD300 回归。每会话仅创建一项文件 Task，未观察到此次重复创建 |

M03 WebSocket 继续保留：本次未复现原 800 帧/秒级输入积压；不独立调查、不宣布修好，留给用户跨环境比较。M01 首音前取消在 S1 G19 出现，但没有原约 4 秒 buffer-wait 超时。M02 原 NATIVE_RUNTIME_CLOSED 恢复链未出现，S2 是用户退出后重开。M05 本次没有重复 Task；M07 UI 用时口径未核定；M10 旧启动 502 不属于本次语音窗口。

两个文件哈希均与 Task result 登记一致，确认真实落盘，但不能据此判定全部内容满足需求。任务保持 revision 1，没有后续替代任务落实餐饮调整。

## 错误去重与根因范围

全日志候选共 19 行 ERROR/CRITICAL/Traceback，包含日志重复、启动及更早会话；没有找到归属于 S1/S2 的 ERROR/CRITICAL 异常栈。更早会话 `web_1a08ab9ee89_ae0f8a1a69f0` 的 provider start 取消排除。业务拒绝仍然存在，不能把“无该等级栈”说成“无错误”。

S1/S2 诊断分别有 16/13 行失败候选，不是 29 个独立故障：

- S1 G19 同一取消链包含 SYNTHESIS_ALREADY_TERMINAL 三次、嵌套 CancelledError 两行、body read failed；随后 notification.prepare 因前台占用延后。
- 关闭/切页时有 NOTIFICATION_CONSUMER_DETACHED；S1 一次和 S2 两次关闭态 context/authority 读取各形成三层 NATIVE_RUNTIME_CAPABILITY_REJECTED。按关联请求去重，不计作多次独立坏实例。
- S2 第二次 user_exit 同期出现 PRODUCT_P2_ROUTE_NOT_FOUND 和 ACTIVATION_GENERATION_STALE；属于生命周期清理竞态候选，额外用户影响未证明。
- S2 G18 TTS body read failed 前已有 synthesis completed，随后 Task voice ACK 成功，不能直接判成未播报。

关闭请求均能关联 `live-voice-p2-close-user_exit-...`。具体拒绝事实、Task command settlement 和产物缺项已有证据；网络设备原因、模型/VAD 单因果、每次查询内部瓶颈和物理听感不在当前证据闭合范围。

## 收尾

本轮只做离线数据一致性与文档检查：输入快照 SHA256、Task 产物哈希、逐会话关联、计时口径、受影响 Markdown 链接和 `git diff --check`。不运行新的产品验证。原始证据及分析脚本留在本机忽略目录，Git 只记录本说明及 STATUS 当前结论。跨环境验证、问题分类和修复决策由用户继续进行。
