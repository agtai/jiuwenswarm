# Explicit Atlas Demo routing

Scope: preserve the selected expense/repurchase executor through the authenticated
callback and Bridge RPC; do not infer it again from rewritten prose. Recognize
Chinese 重购, retain negation, reject unrecognized explicit requests before task
creation/streaming. Unmatched ordinary prose cannot fall through to a prior Demo.
Same call ID with changed selector is a conflict. Native Work/Task routing stays
native; no Demo concurrency expansion and no auto-approval or retry.

Tier 3 local callback field extension, Tier 2 routing/receipt semantics. Validate
selectors against enabled capabilities before execution, carry known no-start
refusals explicitly; unknown transport outcomes stay unknown. Verify exact
selector forwarding, expense-to-coffee switching, original constraints, stale
route zero execution, changed-ID-content fence, native routing, and existing
approvals. Independent review required. Microphone acceptance separate.

Validation closure:
- Combined router tests: 6 passed, including real Bridge expense completion ->
  rejected ambiguous coffee (no new chat.done) -> real coffee mandate creation,
  distinct IDs and old report retrieval. Coffee executor is isolated at stream
  in this test; no user shop purchase or live weather query was performed.
- Bridge callback suite + combined: 15 passed before final additional mock-entry
  test; explicit selector callback test passed. Resolver + callback + combined
  batch: 48 passed / 11 subtests before added boundary regressions.
- Desktop business/local server: 25 passed, including real Python bridge.
  Typecheck/build, scoped ESLint and formatting passed.
- Swarm Atlas host, native default, approvals, instructions: 51 passed.
  Native general Work and Task dispatch stays outside the Demo host.
- Independent read-only review identified @mention bypass and mock multi-demo
  fallback. Both fixed, regressions passed, reviewer verified no remaining
  scoped blockers. Unknown transport outcomes are never auto-retried.
- Existing Windows POSIX mode assertion config_pair_and_close excluded, as before.
- Does not introduce parallel Demo execution; no remote push. Provider microphone
  acceptance remains a separate user trial after loading the new processes.
