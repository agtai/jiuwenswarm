# Instruction optimization — 2026-09-05

> Accepted input: the user's “优化” following the [instruction audit](INSTRUCTION_AND_SKILL_AUDIT_20260905.md).
> Baseline: `8db0a7036ebd9565c2101942cc7e1e9882689dd7`. This is a scoped execution/verification record, not a new product queue or acceptance result.

## Intended outcome and scope

Keep repository and skill guidance useful for completing the authorized work,
with contextual reads, clear completion, proportional verification and no
redundant approval gates. Preserve exact remote authorization, task/result
authority, private configuration, real-path acceptance and unresolved product
limitations.

| Boundary | Owned surfaces | Risk and verification |
|---|---|---|
| Development instructions and document routes | Root AGENTS, Live Voice README/STATUS/documentation rules, current acceptance/showcase/runbook, implicated historical pointers | Tier 0: full scoped diff, links/anchors and authority consistency |
| Bundled skill instructions | 21 SKILL.md roots, necessary references/agent guidance, Project Maintainer UI metadata, AGENT_EN/ZH templates and UI E2E skill routing | Tier 1 for routing/authoring; Tier 2 review for history scope, external calls, output-target protection and background completion instructions. Static validation and independent representative decision probes; no runtime execution claim |

Dependencies: accepted audit findings, current launcher/source behavior,
TESTING's existing risk policy and the installed-skill/source-copy distinction.
This batch adds no classifier, protocol, schema, executable skill changes,
runtime flags or new external permissions.

Exclusions: third-party plugin cache/configuration, private installed Agent/skill
copies, deployment/service restarts, actual history retrieval/paid optimization,
real Agent/Tool/audio journeys, feature-complete or production acceptance,
remote-ref updates. Existing immutable evidence remains historical.

## Execution

- [x] Align repository workflow rules and reduce default reading.
- [x] Align current rehearsal routes, consent wording and historical pointers.
- [x] Refactor bundled skill entrypoints and preserve mode-specific mechanics.
- [x] Validate links/metadata, review the complete scoped changes, and probe
  representative positive, negative and authority-boundary decisions.
- [x] Record actual results and exclusions below. Commit identities are reported
  from Git at handoff, not predicted in this record.

Main is the only repository writer and Git owner. Supporting agents may prepare
non-overlapping skill changes in isolated scratch directories and return them
for Main's review; they may not modify this checkout, stage, commit or integrate.
The available skill workflows supplement this record and TESTING; they do not
add another approval, full-suite, micro-commit or automatic publication gate.

## Verification and outcome

### Delivered changes

| Audit findings | Scoped disposition |
|---|---|
| F01–F02, F12 | Root instructions retain user/repository authority and proportional TESTING requirements; generic skill workflows add no repeated approval, full-read/full-suite or fixed-loop gate. Third-party plugin packages are unchanged. |
| F03–F04 | README routes to the judgement/current packet; STATUS keeps capability consequences and open acceptance. Candidate history is explicitly historical. No PARTIAL/physical gate was promoted. |
| F05–F06 | Runbook documents actual generation-interruption opt-in. Showcase contains the current twelve-step A/B/A2 journey, lawful timing windows, offline completion/notification, artifact comparison and 09:00 → 08:40 fixture arithmetic. Rehearsal and complete-candidate credit remain distinct. |
| F07–F08 | Historical stage/signing routes cannot activate current work; D1 remains unsupported by the declared D0/D2 profile. Three original anchors and a newly reviewed A/B/A2 anchor are corrected. |
| F09–F11 | All 21 bundled roots now route by task; substantial conditional mechanics use references. Context lookup cannot initiate a maintenance audit, history stays within requested scope, and starting detached work cannot masquerade as completion. CLI and template examples follow actual source. |
| F13 | Repository templates/source skills are revised. Existing installed Agent/skill copies, current host plugin caches and runtime prompts are not synchronized or claimed fixed. |

The 21 roots total **4,235 → 615 lines**; their discovery descriptions total
**5,294 → 1,504 characters**. These are source-text counts, not measured prompt
tokens, latency or model-quality gains. Eighteen new conditional references retain
task-specific contracts. Skill names and supported metadata remain unchanged;
`doc-update` deliberately removes its redundant keyword-only `trigger` field,
with discovery expressed in `description`.

Independent review found and corrected: the A/B/A2 anchor, runbook §2.1 routing,
Omni environment-check side effects, destination/overwrite and cleanup examples,
PPT paper-source/whitespace contradictions, explicit user-template parameters,
and the report CLI's weekly/AI limitations. `weekly` still implements a single
day and ignores `--no-ai`; this batch documents that limitation rather than
claiming an executable repair. Omni examples now retain the same verified output
root through save and finalization, including empty-image/empty-script branches.

### Executed verification

- UTF-8/YAML and actual `SkillManager._parse_skill_md` source-method checks pass
  for 21 roots plus the UI E2E root. The parser methods were isolated from the
  service module to avoid runtime initialization; this is parser compatibility,
  not proof of an installed runtime loading the new instructions.
- Both existing `quick_validate.py` implementations were exercised. Fifteen
  roots pass each validator. Six retain identical baseline incompatibilities:
  `advanced-daily-report`, `cross-channel-history-retrieval`, `gitcode-api`,
  `skill-creator`, `swarmskill-creator` use JiuwenSwarm metadata outside those
  generic validators; `openJiuwen-DeepSearch` retains its existing mixed-case
  identity. No new validator regression; no schema/identity was renamed merely
  to satisfy the generic checker.
- Explicit local Markdown targets/anchors were checked across all 182 Live Voice
  Markdown documents and changed instruction files. No unresolved target or
  anchor remains. Fenced code examples are excluded from link interpretation.
- Main reviewed the complete scoped changes. Independent review covered current
  Live Voice authority/routes and all 13 domain-skill changes, including affected
  script/schema facts and downstream references. Identified fixes were rechecked
  against source and the affected command branches.
- An independent six-case decision rehearsal exercised public-history non-use,
  fixed-session/day empty search, running research/old output/failed export,
  business failure without optimizer authorization, one authorized pending-to-done
  JIT without deployment, and context lookup without a project-map artifact.
  Decisions preserved scope, truthful partial/pending results and existing
  authorization; no unrelated history, external calls, duplicate launch,
  proposal acceptance or artifact initialization was selected. These were
  simulated tool-result decisions, not executed business or provider journeys.
- `git diff --check` / staged whitespace checks and the Markdown/metadata-only
  file allowlist are the commit checks. No executable source/test/script was
  changed; full backend/frontend builds or live audio tests are not applicable
  to this instruction-source batch.

Time/order, recovery and identity were checked at the instruction-decision level
where applicable. Actual concurrency, protocol durability, device/Provider and
cross-module business execution remain outside this batch; this record grants
none of their acceptance credit. Existing controlled audit evidence/signature
mechanics remain conditional and unchanged, while retired W2 Gate tooling stays
retired.

Local scratch validation used the existing `.venv` and temporary files outside
the repository. No private credential/configuration changes, installation,
service restart, runtime task mutation or remote-ref update was performed.
