# AgentCore dependency

The former Responses-only patch and wheel builder are retired. The equivalent
fix is included in the pinned AgentCore source used by `pyproject.toml` and
`uv.lock`; Git retains the old reconstruction history.

Use the [AgentCore source guide](../../live-voice/runbooks/AGENTCORE_SOURCE.md).
No copied SDK patches or manual wheel replacement are required.
